eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

server:
  port: 8000
spring:
  application:
    name: Menu-Service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: opp


